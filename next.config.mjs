/** @type {import('next').NextConfig} */
const nextConfig = {
  // Data is generated fresh per request relative to the server's current date,
  // so nothing should be statically cached.
};

export default nextConfig;
